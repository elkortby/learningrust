pub fn run() {
	println!("{}", "data types");
	
	// Default int is "i32"
	// let x = -1;
	// // U => int is +
	// let y: u32 = 5;
	// println!("{}", std::i32::MIN);
	// println!("{}", std::i32::MAX);

	// boolean

	let mut is_active = true;
	println!("{}", is_active);

	is_active = false;
	println!("{}", is_active)
}